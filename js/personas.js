const { createApp } = Vue

  createApp({
    data() {
      return {
        url:"https://juanvi.pythonanywhere.com/personas",
        personas:[],
        error:false,
        cargando:true
      }
    },
    // Se llama después de que la instancia haya 
    // terminado de procesar todas las opciones relacionadas con el estado.
    created() {
        this.fetchData(this.url)
    },
    methods: {
        fetchData(url) {
            fetch(url)
                .then(response => response.json())
                .then(data => {
                    this.personas = data;
                    this.cargando=false
                })
                .catch(err => {
                    console.error(err);
                    this.error=true              
                })
        },
        eliminar(persona) {
            const url = 'https://juanvi.pythonanywhere.com/personas/' + persona; /*producto es el id del persona*/
            var options = {
                method: 'DELETE',
            }
            fetch(url, options)
                .then(res => res.text()) // or res.json()
                .then(res => {
                    location.reload();
                })
        }
    },
  }).mount('#app')